// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import { isJqError, maybeFilter } from 'unified-data-library-mcp/filtering';
import { Metadata, asErrorResult, asTextContentResult } from 'unified-data-library-mcp/tools/types';

import { Tool } from '@modelcontextprotocol/sdk/types.js';
import Unifieddatalibrary from 'unified-data-library';

export const metadata: Metadata = {
  resource: 'onorbitassessment',
  operation: 'read',
  tags: [],
  httpMethod: 'get',
  httpPath: '/udl/onorbitassessment',
};

export const tool: Tool = {
  name: 'list_onorbitassessment',
  description:
    "When using this tool, always use the `jq_filter` parameter to reduce the response size and improve performance.\n\nOnly omit if you're sure you don't need the data.\n\nService operation to dynamically query data by a variety of query parameters not specified in this API documentation. See the queryhelp operation (/udl/&lt;datatype&gt;/queryhelp) for more details on valid/required query parameter information.\n\n# Response Schema\n```json\n{\n  type: 'array',\n  items: {\n    $ref: '#/$defs/onorbitassessment_list_response'\n  },\n  $defs: {\n    onorbitassessment_list_response: {\n      type: 'object',\n      description: 'Spacecraft characterization results from analysis of MASINT data. Supports results of both the overall assessment of a particular spacecraft as well as of individual signature packages.',\n      properties: {\n        assmtTime: {\n          type: 'string',\n          description: 'The time of the assessment, in ISO 8601 UTC format with millisecond precision.',\n          format: 'date-time'\n        },\n        classificationMarking: {\n          type: 'string',\n          description: 'Classification marking of the data in IC/CAPCO Portion-marked format.'\n        },\n        dataMode: {\n          type: 'string',\n          description: 'Indicator of whether the data is REAL, TEST, EXERCISE, or SIMULATED data:\\n\\nREAL:&nbsp;Data collected or produced that pertains to real-world objects, events, and analysis.\\n\\nTEST:&nbsp;Specific datasets used to evaluate compliance with specifications and requirements, and for validating technical, functional, and performance characteristics.\\n\\nEXERCISE:&nbsp;Data pertaining to a government or military exercise. The data may include both real and simulated data.\\n\\nSIMULATED:&nbsp;Synthetic data generated by a model to mimic real-world datasets.',\n          enum: [            'REAL',\n            'TEST',\n            'SIMULATED',\n            'EXERCISE'\n          ]\n        },\n        source: {\n          type: 'string',\n          description: 'Source of the data.'\n        },\n        id: {\n          type: 'string',\n          description: 'Unique identifier of the record, auto-generated by the system.'\n        },\n        assessment: {\n          type: 'string',\n          description: 'The overall assessment of the on-orbit object. The assessment will vary depending on the assessment level. If assmtLevel = SATELLITE, then expected values include HEALTHY, NO DATA, UNHEALTHY, and UNKNOWN. If assmtLevel = SIGNATURE, then expected values include ANOMALOUS, BAD, NOMINAL, and UNKNOWN.'\n        },\n        assmtDetails: {\n          type: 'string',\n          description: 'Comments and details concerning this assessment.'\n        },\n        assmtLevel: {\n          type: 'string',\n          description: 'The level (SATELLITE, SIGNATURE) of this assessment.'\n        },\n        assmtRotPeriod: {\n          type: 'number',\n          description: 'The rotational period, in seconds, determined in the assessment of the on-orbit object.'\n        },\n        assmtSubType: {\n          type: 'string',\n          description: 'The sub-type for this assessment. The sub-type provides an additional level of specification when the assessment level = SIGNATURE, and will vary based on the overall assessment. If assessment = ANOMALOUS, then expected values include ACTIVITY OBSERVED, BAD CONFIGURATION, MANEUVERING, OTHER, POSSIBLE SAFE MODE, UNSTABLE, and WRONG ATTITUDE. If assessment = BAD, then expected values include BAD-MISSING STATE VECTOR, CORRUPT-NOISY, CROSS-TAG, DROPOUTS, INSUFFICIENT DATA, INSUFFICIENT LOOK ANGLE, NO CROSSOVER, and SHORT. If assessment = NOMINAL, then expected values include ACTIVITY OBSERVED, GRAVITY GRADIENT, HORIZON STABLE, INERTIAL, MANEUVERING, SPIN STABLE, and STABLE. If assessment = UNKNOWN, then expected values include NO COHORT, and OTHER.'\n        },\n        assmtURL: {\n          type: 'string',\n          description: 'URL to an external location containing additional assessment information.'\n        },\n        autoAssmt: {\n          type: 'boolean',\n          description: 'Flag indicating whether this assessment was auto-generated (true) or produced by an analyst.'\n        },\n        collectionURL: {\n          type: 'string',\n          description: 'URL to an external location containing the data that was used to make this assessment.'\n        },\n        components: {\n          type: 'array',\n          description: 'Array of the affected spacecraft component(s) relevant to this assessment, if non-nominal.',\n          items: {\n            type: 'string'\n          }\n        },\n        createdAt: {\n          type: 'string',\n          description: 'Time the row was created in the database, auto-populated by the system.',\n          format: 'date-time'\n        },\n        createdBy: {\n          type: 'string',\n          description: 'Application user who created the row in the database, auto-populated by the system.'\n        },\n        idOnOrbit: {\n          type: 'string',\n          description: 'Unique identifier of the target satellite on-orbit object to which this assessment applies. This ID can be used to obtain additional information on an OnOrbit object using the \\'get by ID\\' operation (e.g. /udl/onorbit/{id}). For example, the OnOrbit with idOnOrbit = 25544 would be queried as /udl/onorbit/25544.'\n        },\n        idSensor: {\n          type: 'string',\n          description: 'Unique identifier of the sensor from which the signature data applied in this assessment was collected. This ID can be used to obtain additional information on a sensor using the \\'get by ID\\' operation (e.g. /udl/sensor/{id}). For example, the sensor with idSensor = abc would be queried as /udl/sensor/abc.'\n        },\n        obDuration: {\n          type: 'number',\n          description: 'The total duration, in hours, of the signature or set of signatures used to create this assessment.'\n        },\n        obTime: {\n          type: 'string',\n          description: 'The observation time, in ISO 8601 UTC format with millisecond precision. For non-instantaneous collections, the observation time will correspond to the earliest timestamp in the signature or signature set data.',\n          format: 'date-time'\n        },\n        origin: {\n          type: 'string',\n          description: 'Originating system or organization which produced the data, if different from the source. The origin may be different than the source if the source was a mediating system which forwarded the data on behalf of the origin system. If null, the source may be assumed to be the origin.'\n        },\n        origNetwork: {\n          type: 'string',\n          description: 'The originating source network on which this record was created, auto-populated by the system.'\n        },\n        origObjectId: {\n          type: 'string',\n          description: 'Optional identifier provided by the source to indicate the target on-orbit object to which this assessment applies. This may be an internal identifier and not necessarily map to a valid satellite number.'\n        },\n        origSensorId: {\n          type: 'string',\n          description: 'Optional identifier provided by the observation source to indicate the sensor which produced this observation. This may be an internal identifier and not necessarily a valid sensor ID.'\n        },\n        satNo: {\n          type: 'integer',\n          description: 'Satellite/Catalog number of the target on-orbit object to which this assessment applies.'\n        },\n        sigDataType: {\n          type: 'string',\n          description: 'The observation data type (LONG DWELL, NARROWBAND, PHOTOMETRIC POL, PHOTOMETRY, RANGE PROFILER, WIDEBAND, etc.) contained in the signature. Applies when assmtLevel = SIGNATURE.'\n        },\n        sourceDL: {\n          type: 'string',\n          description: 'The source data library from which this record was received. This could be a remote or tactical UDL or another data library. If null, the record should be assumed to have originated from the primary Enterprise UDL.'\n        },\n        srcIds: {\n          type: 'array',\n          description: 'Array of UUIDs of the UDL data records that are related to this assessment. See the associated \\'srcTyps\\' array for the specific types of data, positionally corresponding to the UUIDs in this array. The \\'srcTyps\\' and \\'srcIds\\' arrays must match in size. See the corresponding srcTyps array element for the data type of the UUID and use the appropriate API operation to retrieve that object.',\n          items: {\n            type: 'string'\n          }\n        },\n        srcTyps: {\n          type: 'array',\n          description: 'Array of UDL record types (DOA, ELSET, EO, ESID, GROUNDIMAGE, POI, MANEUVER, MTI, RADAR, RF, SIGACT, SKYIMAGE, SV, TRACK, etc.) that are related to this assessment. See the associated \\'srcIds\\' array for the record UUIDs, positionally corresponding to the record types in this array. The \\'srcTyps\\' and \\'srcIds\\' arrays must match in size.',\n          items: {\n            type: 'string'\n          }\n        },\n        transactionId: {\n          type: 'string',\n          description: 'Optional identifier to track a commercial or marketplace transaction executed to produce this data.'\n        }\n      },\n      required: [        'assmtTime',\n        'classificationMarking',\n        'dataMode',\n        'source'\n      ]\n    }\n  }\n}\n```",
  inputSchema: {
    type: 'object',
    properties: {
      assmtTime: {
        type: 'string',
        description:
          'The time of the assessment, in ISO 8601 UTC format with millisecond precision. (YYYY-MM-DDTHH:MM:SS.sssZ)',
        format: 'date-time',
      },
      firstResult: {
        type: 'integer',
      },
      maxResults: {
        type: 'integer',
      },
      jq_filter: {
        type: 'string',
        title: 'jq Filter',
        description:
          'A jq filter to apply to the response to include certain fields. Consult the output schema in the tool description to see the fields that are available.\n\nFor example: to include only the `name` field in every object of a results array, you can provide ".results[].name".\n\nFor more information, see the [jq documentation](https://jqlang.org/manual/).',
      },
    },
    required: ['assmtTime'],
  },
  annotations: {
    readOnlyHint: true,
  },
};

export const handler = async (client: Unifieddatalibrary, args: Record<string, unknown> | undefined) => {
  const { jq_filter, ...body } = args as any;
  const response = await client.onorbitassessment.list(body).asResponse();
  try {
    return asTextContentResult(await maybeFilter(jq_filter, await response.json()));
  } catch (error) {
    if (error instanceof Unifieddatalibrary.APIError || isJqError(error)) {
      return asErrorResult(error.message);
    }
    throw error;
  }
};

export default { metadata, tool, handler };
