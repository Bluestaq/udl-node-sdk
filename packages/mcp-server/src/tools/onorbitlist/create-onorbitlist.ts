// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import { Metadata, asTextContentResult } from 'unified-data-library-mcp/tools/types';

import { Tool } from '@modelcontextprotocol/sdk/types.js';
import Unifieddatalibrary from 'unified-data-library';

export const metadata: Metadata = {
  resource: 'onorbitlist',
  operation: 'write',
  tags: [],
  httpMethod: 'post',
  httpPath: '/udl/onorbitlist',
};

export const tool: Tool = {
  name: 'create_onorbitlist',
  description:
    'Service operation to take a single OnOrbitList as a POST body and ingest into the database. A specific role is required to perform this service operation. Please contact the UDL team for assistance.',
  inputSchema: {
    type: 'object',
    properties: {
      classificationMarking: {
        type: 'string',
        description: 'Classification marking of the data in IC/CAPCO Portion-marked format.',
      },
      dataMode: {
        type: 'string',
        description:
          'Indicator of whether the data is EXERCISE, REAL, SIMULATED, or TEST data:\n\nEXERCISE:&nbsp;Data pertaining to a government or military exercise. The data may include both real and simulated data.\n\nREAL:&nbsp;Data collected or produced that pertains to real-world objects, events, and analysis.\n\nSIMULATED:&nbsp;Synthetic data generated by a model to mimic real-world datasets.\n\nTEST:&nbsp;Specific datasets used to evaluate compliance with specifications and requirements, and for validating technical, functional, and performance characteristics.',
        enum: ['REAL', 'TEST', 'SIMULATED', 'EXERCISE'],
      },
      name: {
        type: 'string',
        description: 'Unique name of the list.',
      },
      onOrbitListItems: {
        type: 'array',
        description:
          'This is a list of onOrbitListItems that will be related one-to-one with an onOrbit entry.',
        items: {
          type: 'object',
          description: 'Items associated with this onOrbitList record.',
          properties: {
            clearingBoxCrossTrack: {
              type: 'number',
              description:
                'Height of a box, in degrees, volume expected to be cleared by sensor providers, if CLEARING is selected.',
            },
            clearingBoxInTrack: {
              type: 'number',
              description:
                'Width of a box volume, in degrees, expected to be cleared by sensor providers, if CLEARING is selected.',
            },
            clearingRadius: {
              type: 'number',
              description:
                'Radius, in degrees, of a spherical volume expected to be cleared by sensor providers, if CLEARING is selected.',
            },
            commonName: {
              type: 'string',
              description: 'Common name of the onorbit object.',
            },
            countryCode: {
              type: 'string',
              description:
                'This value is typically the ISO 3166 Alpha-3 three-character country code, however it can also represent various consortiums that do not appear in the ISO document.',
            },
            expiredOn: {
              type: 'string',
              description:
                'Datetime expiration of a satellite on this list, allowing for the maintenance of a history of when satellites entered and when they exited the list in ISO 8601 UTC datetime format with millisecond precision.',
              format: 'date-time',
            },
            freqMins: {
              type: 'number',
              description:
                'Frequency of additional routine, in minutes, tasking identified in and corresponding to the monitoringType array.',
            },
            monitoringType: {
              type: 'string',
              description:
                'Routine tasking that should be applied to this object.  REVISIT_RATE allows users to define custom revisit rates for individual satellites, HVA_CLEARING allows users to define custom volumes that are expected to be clear of unknown objects, and POL would be collects on a specified increment in support of collecting data that feeds into Pattern of Life (PoL) assessments.',
            },
            objectId: {
              type: 'string',
              description:
                'Unique identifier of the on-orbit object. This is typically the USSF 18th SDS satellite number (also sometimes referred to as NORAD ID/number) but could be an identifier from another satellite catalog namespace. See the ‘namespace’ field for the appropriate identifier namespace. If namespace is null, 18SDS satellite number is assumed.',
            },
            orbitRegime: {
              type: 'string',
              description:
                "Orbit Regime refers to a classification of a satellite's orbit based on its altitude, inclination, and other orbital characteristics. Common orbit regimes include Low Earth Orbit (LEO), Medium Earth Orbit (MEO), Geostationary Orbit (GEO), and Highly Elliptical Orbit (HEO).",
            },
            origObjectId: {
              type: 'string',
              description:
                'Optional identifier indicates the on-orbit object being referenced. This may be an internal system identifier and not necessarily a valid satellite number.',
            },
            payloadPriority: {
              type: 'number',
              description:
                'Payload priority based on the type of payload that has been identified or that is suspected. Priority of the payload as a number. (1=highest priority).',
            },
            rank: {
              type: 'integer',
              description:
                'Rank refers to the assigned position or level of priority given to a satellite based on its relative importance, urgency, or operational relevance as determined by the applicable operations unit.',
            },
            urgency: {
              type: 'number',
              description:
                'Tasking urgency, typically will be on a 1-10 scale.  Urgency as a number. (1=highest priority).',
            },
          },
        },
      },
      source: {
        type: 'string',
        description: 'Source of the data.',
      },
      id: {
        type: 'string',
        description: 'Unique identifier of the record, auto-generated by the system.',
      },
      defaultRevisitRateMins: {
        type: 'number',
        description: 'Default revisit rate in minutes for all objects in this list.',
      },
      description: {
        type: 'string',
        description: 'Description of the list.',
      },
      listPriority: {
        type: 'number',
        description:
          'Numerical priority of this orbit list relative to other orbit lists; lower values indicate higher priority. Decimal values allowed for fine granularity.  Consumers should contact the provider for details on the priority.',
      },
      namespace: {
        type: 'string',
        description:
          'Defined naming system that ensures each satellite or space object has a unique and unambiguous identifier within the name space (e.g. JCO, 18SDS). If null, it is assumed to be 18th Space Defense Squadron (18SDS).',
      },
      origin: {
        type: 'string',
        description:
          'Originating system or organization which produced the data, if different from the source. The origin may be different than the source if the source was a mediating system which forwarded the data on behalf of the origin system. If null, the source may be assumed to be the origin.',
      },
      tags: {
        type: 'array',
        description:
          'Optional array of provider/source specific tags for this data, where each element is no longer than 32 characters, used for implementing data owner conditional access controls to restrict access to the data. Should be left null by data providers unless conditional access controls are coordinated with the UDL team.',
        items: {
          type: 'string',
        },
      },
      transactionId: {
        type: 'string',
        description:
          'Optional identifier to track a commercial or marketplace transaction executed to produce this data.',
      },
    },
    required: ['classificationMarking', 'dataMode', 'name', 'onOrbitListItems', 'source'],
  },
  annotations: {},
};

export const handler = async (client: Unifieddatalibrary, args: Record<string, unknown> | undefined) => {
  const body = args as any;
  const response = await client.onorbitlist.create(body).asResponse();
  return asTextContentResult(await response.text());
};

export default { metadata, tool, handler };
